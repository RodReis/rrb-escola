.next
node_modules
.env
.env.local
.env*.local
coverage
dist
supabase/.branches
supabase/.temp
tsconfig.tsbuildinfo
*.log
.next-dev*.log
.next-dev*.err.log
backups/
docs/pdfs/reconcile-2026-*.json
docs/pdfs/backup-matriculas-2026-*.json
docs/pdfs/*.dump
.superpowers/
.vercel

# Artefatos de ferramentas locais (impeccable, playwright MCP)
.impeccable/
.playwright-mcp/

# Dados pessoais de alunos (CPF, RG, endereco, telefone, foto de menores).
# Ficam so na maquina de quem opera a importacao — nunca versionados.
dados-alunos/

# public/ e servido estaticamente pelo Next: planilha ou ficha de aluno
# largada aqui vira download, bastando o middleware deixar de cobrir estatico.
public/*.pdf
public/*.xlsx
public/*.json
!public/face-models/*.json

# Fichas exportadas do Escolar Manager
docs/pdfs/*.pdf
docs/pdfs/*.txt

# Bytecode Python dos scripts de importacao
__pycache__/
*.pyc
