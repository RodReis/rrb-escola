# Férias e 13º (Folha v2.1) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Runs especiais de 13º (1ª/2ª parcela) e férias no motor da Folha v2, com períodos aquisitivos, baixa de provisões, recibos PDF e geração automática.

**Architecture:** Reuso total do pipeline v2 (`folha_runs` ganha `tipo`; mesmos itens/lançamentos/estados/conciliação). Fórmulas novas em módulos puros do engine (`decimo.ts`, `ferias.ts`); runs especiais são montadas pelo serviço com essas fórmulas — o pipeline mensal não muda. Spec: `docs/superpowers/specs/2026-06-12-ferias-13o-design.md`.

**Tech Stack:** idem v2 (Next.js 14, Supabase, Vitest, jsPDF, ExcelJS).

**Pré-requisito:** Plano `2026-06-12-folha-v2.md` executado até a Fase 4 (inclusive) + ADENDO 1 aplicado. Este plano referencia funções/tabelas criadas lá (`calcINSS`, `calcIrrf2026`, `folha_runs`, `gerarRun`, `recalcularTotaisRun`, dispatcher).

---

## Estrutura de arquivos

| Arquivo | Responsabilidade |
|---|---|
| `supabase/migrations/202606130001_folha_v21_ferias_13o.sql` | `tipo` em runs, `folha_periodos_aquisitivos`, campos novos, rubricas seed |
| `src/lib/folha/engine/avos.ts` | `avos()`, `mediaBase12()`, `baseCalculo13Ferias()` |
| `src/lib/folha/engine/decimo.ts` | `calcularDecimo1a()`, `calcularDecimo2a()` (INSS/IRRF exclusivos) |
| `src/lib/folha/engine/ferias.ts` | `calcularFerias()`, `descontoGozoNaMensal()` |
| `src/lib/folha/aquisitivos.ts` | Serviço de períodos aquisitivos (criar, agendar, abrir próximo, marcar vencidos) |
| `src/lib/folha/runs-especiais.ts` | `gerarRunEspecial()`, baixa de provisões, validações por tipo |
| `src/lib/actions/folha-especiais.ts` | Actions: agendar gozo, gerar run especial manual |
| `src/lib/actions/folha-jobs.ts` (modify) | Jobs novos: gerar 13º, gerar férias, alertas aquisitivo |
| `src/lib/folha/service.ts` (modify) | Injeção de `ferias_desconto_gozo` na folha mensal |
| `src/app/(app)/rh/folha-v2/ferias/page.tsx` | Agenda de férias |
| `src/lib/folha/holerite-pdf.ts` (modify) | Títulos por tipo, assinatura em férias, avos na referência |
| `src/lib/folha/pacote-contador.ts` (modify) | Aba `Ferias-13o` |
| `scripts/importar_bases_historicas.mjs` | One-off: bases 2025/26 para média de 12 meses (condicional) |

---

### Task 1: Migration v2.1

**Files:**
- Create: `supabase/migrations/202606130001_folha_v21_ferias_13o.sql`

- [ ] **Step 1: Escrever a migration**

```sql
alter table public.folha_runs add column if not exists tipo text not null default 'mensal'
  check (tipo in ('mensal','decimo_1a','decimo_2a','ferias'));
alter table public.folha_runs drop constraint if exists folha_runs_company_id_competencia_key;
create unique index if not exists folha_runs_company_comp_tipo
  on public.folha_runs (company_id, competencia, tipo);

create table if not exists public.folha_periodos_aquisitivos (
  id uuid primary key default gen_random_uuid(),
  contrato_id uuid not null references folha_contratos(id) on delete cascade,
  inicio date not null,
  fim date not null,
  dias_direito int not null default 30,
  janela text,
  gozo_inicio date,
  gozo_dias int,
  dias_abono int not null default 0 check (dias_abono in (0, 10)),
  status text not null default 'aberto' check (status in ('aberto','agendado','gozado','vencido')),
  run_id uuid references folha_runs(id) on delete set null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (contrato_id, inicio)
);
alter table public.folha_periodos_aquisitivos enable row level security;
create policy folha_aquisitivos_rw on folha_periodos_aquisitivos for all
  using (exists (select 1 from folha_contratos c where c.id = contrato_id
    and c.escola_id = (current_perfil()).escola_id and (current_perfil()).perfil in ('admin','financeiro')));
drop trigger if exists folha_aquisitivos_updated_at on folha_periodos_aquisitivos;
create trigger folha_aquisitivos_updated_at before update on folha_periodos_aquisitivos
for each row execute function public.set_updated_at();

alter table public.folha_contratos
  add column if not exists antecipa_13_com_ferias boolean not null default false,
  add column if not exists janela_ferias text;

alter table public.folha_config
  add column if not exists ferias_janelas jsonb not null default
    '[{"codigo":"julho","mes_gozo":7,"mes_pagamento":6},{"codigo":"dezembro","mes_gozo":1,"mes_pagamento":12}]',
  add column if not exists decimo_1a_prazo text not null default '11-30',
  add column if not exists decimo_2a_prazo text not null default '12-20',
  add column if not exists decimo_gerar_dia jsonb not null default '{"decimo_1a":"11-01","decimo_2a":"12-01"}',
  add column if not exists ferias_gerar_antes_dias int not null default 30,
  add column if not exists ferias_pagar_antes_dias int not null default 2,
  add column if not exists alerta_aquisitivo_dias jsonb not null default '[60,30]',
  add column if not exists base_13_ferias jsonb not null default
    '{"clt_professor":"media_12","clt":"vigente"}';

insert into folha_rubricas (escola_id, codigo, nome, tipo, metodo_calculo,
  incide_inss, incide_irrf, incide_fgts, incide_dsr, ordem_holerite)
select e.id, r.codigo, r.nome, r.tipo, r.metodo, r.inss, r.irrf, r.fgts, false, r.ordem
from escolas e cross join (values
  ('decimo_1a_parcela','13º Salário — 1ª parcela','provento','decimo_1a',false,false,true,10),
  ('decimo_2a_parcela','13º Salário — 2ª parcela','provento','decimo_2a',false,false,true,10),
  ('inss_13','INSS 13º','desconto','inss_13',false,false,false,60),
  ('irrf_13','IRRF 13º','desconto','irrf_13',false,false,false,61),
  ('desconto_adiantamento_13','Adiantamento 13º (1ª parcela)','desconto','auto_decimo_2a',false,false,false,62),
  ('ferias_gozo','Férias','provento','ferias_gozo',true,true,true,10),
  ('ferias_terco','1/3 Constitucional','provento','ferias_terco',true,true,true,11),
  ('abono_pecuniario','Abono pecuniário','provento','abono',false,false,false,12),
  ('abono_terco','1/3 do abono','provento','abono_terco',false,false,false,13),
  ('ferias_desconto_gozo','Desconto férias gozadas','desconto','auto_mensal_gozo',false,false,false,68)
) as r(codigo,nome,tipo,metodo,inss,irrf,fgts,ordem)
on conflict (escola_id, codigo) do nothing;

insert into folha_periodos_aquisitivos (contrato_id, inicio, fim, janela)
select c.id, c.data_admissao, c.data_admissao + interval '12 months', c.janela_ferias
from folha_contratos c
where c.ativo and not exists (select 1 from folha_periodos_aquisitivos p where p.contrato_id = c.id);
```

Nota: o backfill final cria o 1º período aquisitivo a partir da admissão — para funcionários antigos isso gera período já vencido; o job de alertas marcará `vencido` e a tela permite ajustar `inicio` manualmente para o período aquisitivo corrente real (decisão operacional: alinhar com o contador na implantação).

- [ ] **Step 2: Aplicar** — `npx supabase db reset --local` — Expected: sem erro; rubricas novas presentes.
- [ ] **Step 3: Commit**

```bash
git add supabase/migrations/202606130001_folha_v21_ferias_13o.sql
git commit -m "feat(folha-v2.1): schema ferias/13o, periodos aquisitivos e rubricas"
```

### Task 2: Engine — avos e média 12 meses

**Files:**
- Create: `src/lib/folha/engine/avos.ts`
- Test: `src/lib/folha/engine/avos.test.ts`

- [ ] **Step 1: Teste falhando**

```typescript
import { describe, expect, it } from "vitest";
import { avos, mediaBase12, baseCalculo13Ferias } from "./avos";

describe("avos", () => {
  it("ano completo = 12", () => {
    expect(avos("2020-03-10", null, 2026)).toBe(12);
  });
  it("admissao 20/jun (10 dias em junho, <15) = 6 avos (jul-dez)", () => {
    expect(avos("2026-06-20", null, 2026)).toBe(6);
  });
  it("admissao 10/jun (>=15 dias em junho) = 7 avos", () => {
    expect(avos("2026-06-10", null, 2026)).toBe(7);
  });
  it("desligamento 14/out (<15 dias em out) = 9 avos", () => {
    expect(avos("2020-01-01", "2026-10-14", 2026)).toBe(9);
  });
});

describe("mediaBase12", () => {
  it("media de 12 valores", () => {
    expect(mediaBase12(Array(12).fill(3000))).toBe(3000);
  });
  it("menos de 12 meses usa o que existir", () => {
    expect(mediaBase12([3000, 3300, 3600])).toBe(3300);
  });
  it("vazio retorna null (chamador decide fallback)", () => {
    expect(mediaBase12([])).toBeNull();
  });
});

describe("baseCalculo13Ferias", () => {
  const cfg = { clt_professor: "media_12", clt: "vigente" } as Record<string, string>;
  it("professor usa media", () => {
    expect(baseCalculo13Ferias("clt_professor", cfg, 5000, [4000, 4400])).toBe(4200);
  });
  it("clt usa vigente", () => {
    expect(baseCalculo13Ferias("clt", cfg, 5000, [4000])).toBe(5000);
  });
  it("media sem historico cai para vigente", () => {
    expect(baseCalculo13Ferias("clt_professor", cfg, 5000, [])).toBe(5000);
  });
});
```

Run: `npx vitest run src/lib/folha/engine/avos.test.ts` — Expected: FAIL.

- [ ] **Step 2: Implementar**

```typescript
function round2(n: number): number {
  return Math.round(n * 100) / 100;
}

export function avos(admissaoISO: string, desligamentoISO: string | null, anoRef: number): number {
  const adm = new Date(`${admissaoISO}T12:00:00Z`);
  const desl = desligamentoISO ? new Date(`${desligamentoISO}T12:00:00Z`) : null;
  let total = 0;
  for (let mes = 0; mes < 12; mes++) {
    const inicioMes = new Date(Date.UTC(anoRef, mes, 1, 12));
    const fimMes = new Date(Date.UTC(anoRef, mes + 1, 0, 12));
    const ini = adm > inicioMes ? adm : inicioMes;
    const fim = desl && desl < fimMes ? desl : fimMes;
    if (ini > fim) continue;
    const dias = Math.floor((fim.getTime() - ini.getTime()) / 86400000) + 1;
    if (dias >= 15) total++;
  }
  return total;
}

export function mediaBase12(basesAnteriores: number[]): number | null {
  if (basesAnteriores.length === 0) return null;
  const ultimas = basesAnteriores.slice(-12);
  return round2(ultimas.reduce((a, b) => a + b, 0) / ultimas.length);
}

export function baseCalculo13Ferias(
  perfilCodigo: string,
  config: Record<string, string>,
  salarioVigente: number,
  basesAnteriores: number[],
): number {
  if (config[perfilCodigo] === "media_12") {
    return mediaBase12(basesAnteriores) ?? round2(salarioVigente);
  }
  return round2(salarioVigente);
}
```

- [ ] **Step 3: Rodar** — Expected: PASS (10).
- [ ] **Step 4: Commit**

```bash
git add src/lib/folha/engine/avos.ts src/lib/folha/engine/avos.test.ts
git commit -m "feat(folha-v2.1): avos e media de 12 meses"
```

### Task 3: Engine — 13º (cálculo exclusivo)

**Files:**
- Create: `src/lib/folha/engine/decimo.ts`
- Test: `src/lib/folha/engine/decimo.test.ts`

- [ ] **Step 1: Teste falhando**

```typescript
import { describe, expect, it } from "vitest";
import { calcularDecimo1a, calcularDecimo2a } from "./decimo";

const inssFaixas = [
  { ordem: 1, valor_de: 0, valor_ate: 1621.0, aliquota: 0.075, parcela_deduzir: 0 },
  { ordem: 2, valor_de: 1621.0, valor_ate: 2902.84, aliquota: 0.09, parcela_deduzir: 0 },
  { ordem: 3, valor_de: 2902.84, valor_ate: 4354.27, aliquota: 0.12, parcela_deduzir: 0 },
  { ordem: 4, valor_de: 4354.27, valor_ate: 8475.55, aliquota: 0.14, parcela_deduzir: 0 },
];
const irFaixas = [
  { ordem: 1, valor_de: 0, valor_ate: 2428.8, aliquota: 0, parcela_deduzir: 0, deducao_dependente: 189.59 },
  { ordem: 2, valor_de: 2428.8, valor_ate: 2826.65, aliquota: 0.075, parcela_deduzir: 182.16, deducao_dependente: 189.59 },
  { ordem: 3, valor_de: 2826.65, valor_ate: 3751.05, aliquota: 0.15, parcela_deduzir: 394.16, deducao_dependente: 189.59 },
  { ordem: 4, valor_de: 3751.05, valor_ate: 4664.68, aliquota: 0.225, parcela_deduzir: 675.49, deducao_dependente: 189.59 },
  { ordem: 5, valor_de: 4664.68, valor_ate: null, aliquota: 0.275, parcela_deduzir: 908.73, deducao_dependente: 189.59 },
];
const redutor = { limite_isencao: 5000, limite_reducao: 7350 };
const faixas = { inss: inssFaixas, ir: irFaixas };

describe("calcularDecimo1a", () => {
  it("metade da base proporcional aos avos, sem descontos", () => {
    const r = calcularDecimo1a({ base: 3000, avos: 12 });
    expect(r.lancamentos).toEqual([
      { rubrica_codigo: "decimo_1a_parcela", referencia: "12/12 avos", valor: 1500, origem: "auto" },
    ]);
    expect(r.liquido).toBe(1500);
  });
  it("avos parciais: 6/12", () => {
    const r = calcularDecimo1a({ base: 3000, avos: 6 });
    expect(r.lancamentos[0].valor).toBe(750);
  });
});

describe("calcularDecimo2a", () => {
  it("13o cheio - 1a parcela, INSS e IRRF sobre o 13o total (exclusivo)", () => {
    const r = calcularDecimo2a({ base: 3000, avos: 12, valor1aPaga: 1500, dependentes: 0, faixas, redutor });
    const cheio = 3000;
    const lanc = Object.fromEntries(r.lancamentos.map((l) => [l.rubrica_codigo, l.valor]));
    expect(lanc["decimo_2a_parcela"]).toBe(cheio);
    expect(lanc["desconto_adiantamento_13"]).toBe(1500);
    expect(lanc["inss_13"]).toBeCloseTo(1621 * 0.075 + (2902.84 - 1621) * 0.09 + (3000 - 2902.84) * 0.12, 2);
    expect(lanc["irrf_13"]).toBe(0);
    expect(r.liquido).toBeCloseTo(cheio - 1500 - lanc["inss_13"], 2);
  });
  it("sem 1a paga nao gera desconto", () => {
    const r = calcularDecimo2a({ base: 3000, avos: 12, valor1aPaga: 0, dependentes: 0, faixas, redutor });
    expect(r.lancamentos.find((l) => l.rubrica_codigo === "desconto_adiantamento_13")).toBeUndefined();
  });
  it("redutor Lei 15.270 aplica no 13o (rendimento 5500 na zona de reducao)", () => {
    const r = calcularDecimo2a({ base: 5500, avos: 12, valor1aPaga: 0, dependentes: 0, faixas, redutor });
    const irrf = r.lancamentos.find((l) => l.rubrica_codigo === "irrf_13")!.valor;
    expect(irrf).toBeGreaterThan(0);
    const inss13 = r.lancamentos.find((l) => l.rubrica_codigo === "inss_13")!.valor;
    const cheioTabela = (5500 - inss13) * 0.275 - 908.73;
    expect(irrf).toBeLessThan(cheioTabela);
  });
  it("validacao: avos fora de 0-12", () => {
    const r = calcularDecimo2a({ base: 3000, avos: 13, valor1aPaga: 0, dependentes: 0, faixas, redutor });
    expect(r.validacoes.some((v) => v.nivel === "erro")).toBe(true);
  });
});
```

Run: `npx vitest run src/lib/folha/engine/decimo.test.ts` — Expected: FAIL.

- [ ] **Step 2: Implementar**

```typescript
import { calcINSS, type InssBracket, type IrBracket } from "@/lib/payroll/calculators";
import { calcIrrf2026, type RedutorIrrf } from "./irrf";
import type { LancamentoCalculado, ValidacaoItem } from "./types";

function round2(n: number): number {
  return Math.round(n * 100) / 100;
}

export type ResultadoEspecial = {
  lancamentos: LancamentoCalculado[];
  total_proventos: number;
  total_descontos: number;
  liquido: number;
  base_inss: number;
  base_irrf: number;
  base_fgts: number;
  validacoes: ValidacaoItem[];
};

function montar(lancamentos: LancamentoCalculado[], tipos: Record<string, "provento" | "desconto">,
  bases: { inss: number; irrf: number; fgts: number }, validacoes: ValidacaoItem[]): ResultadoEspecial {
  const total_proventos = round2(lancamentos.filter((l) => tipos[l.rubrica_codigo] === "provento")
    .reduce((a, l) => a + l.valor, 0));
  const total_descontos = round2(lancamentos.filter((l) => tipos[l.rubrica_codigo] === "desconto")
    .reduce((a, l) => a + l.valor, 0));
  return {
    lancamentos, total_proventos, total_descontos,
    liquido: round2(total_proventos - total_descontos),
    base_inss: bases.inss, base_irrf: bases.irrf, base_fgts: bases.fgts, validacoes,
  };
}

export function calcularDecimo1a(args: { base: number; avos: number }): ResultadoEspecial {
  const validacoes: ValidacaoItem[] = [];
  if (args.avos < 0 || args.avos > 12) validacoes.push({ nivel: "erro", mensagem: `Avos inválido: ${args.avos}` });
  if (args.base <= 0) validacoes.push({ nivel: "erro", mensagem: "Base do 13º zerada" });
  const valor = round2((args.base / 2) * (args.avos / 12));
  const lancamentos: LancamentoCalculado[] = [
    { rubrica_codigo: "decimo_1a_parcela", referencia: `${args.avos}/12 avos`, valor, origem: "auto" },
  ];
  return montar(lancamentos, { decimo_1a_parcela: "provento" }, { inss: 0, irrf: 0, fgts: valor }, validacoes);
}

export function calcularDecimo2a(args: {
  base: number; avos: number; valor1aPaga: number; dependentes: number;
  faixas: { inss: InssBracket[]; ir: IrBracket[] }; redutor: RedutorIrrf | null;
}): ResultadoEspecial {
  const validacoes: ValidacaoItem[] = [];
  if (args.avos < 0 || args.avos > 12) validacoes.push({ nivel: "erro", mensagem: `Avos inválido: ${args.avos}` });
  if (args.base <= 0) validacoes.push({ nivel: "erro", mensagem: "Base do 13º zerada" });

  const cheio = round2(args.base * (args.avos / 12));
  const inss13 = calcINSS(cheio, args.faixas.inss);
  const baseIrrf = round2(Math.max(0, cheio - inss13));
  const irrf13 = calcIrrf2026({
    baseIr: baseIrrf, rendimento: cheio, dependentes: args.dependentes,
    faixas: args.faixas.ir, redutor: args.redutor,
  });

  const tipos: Record<string, "provento" | "desconto"> = {
    decimo_2a_parcela: "provento", inss_13: "desconto", irrf_13: "desconto",
    desconto_adiantamento_13: "desconto",
  };
  const lancamentos: LancamentoCalculado[] = [
    { rubrica_codigo: "decimo_2a_parcela", referencia: `${args.avos}/12 avos`, valor: cheio, origem: "auto" },
    { rubrica_codigo: "inss_13", referencia: null, valor: inss13, origem: "auto" },
    { rubrica_codigo: "irrf_13", referencia: null, valor: irrf13, origem: "auto" },
  ];
  if (args.valor1aPaga > 0) {
    lancamentos.push({ rubrica_codigo: "desconto_adiantamento_13", referencia: null,
      valor: round2(args.valor1aPaga), origem: "auto" });
  }
  return montar(lancamentos, tipos, { inss: cheio, irrf: baseIrrf, fgts: cheio }, validacoes);
}
```

- [ ] **Step 3: Rodar** — Expected: PASS (6). Conferir: `irrf_13 = 0` no caso base 3000 porque o 13º total fica abaixo da isenção de 5.000 (redutor zera).
- [ ] **Step 4: Commit**

```bash
git add src/lib/folha/engine/decimo.ts src/lib/folha/engine/decimo.test.ts
git commit -m "feat(folha-v2.1): calculo exclusivo do 13o com redutor"
```

### Task 4: Engine — férias

**Files:**
- Create: `src/lib/folha/engine/ferias.ts`
- Test: `src/lib/folha/engine/ferias.test.ts`

- [ ] **Step 1: Teste falhando**

```typescript
import { describe, expect, it } from "vitest";
import { calcularFerias, descontoGozoNaMensal } from "./ferias";

const inssFaixas = [
  { ordem: 1, valor_de: 0, valor_ate: 1621.0, aliquota: 0.075, parcela_deduzir: 0 },
  { ordem: 2, valor_de: 1621.0, valor_ate: 2902.84, aliquota: 0.09, parcela_deduzir: 0 },
  { ordem: 3, valor_de: 2902.84, valor_ate: 4354.27, aliquota: 0.12, parcela_deduzir: 0 },
  { ordem: 4, valor_de: 4354.27, valor_ate: 8475.55, aliquota: 0.14, parcela_deduzir: 0 },
];
const irFaixas = [
  { ordem: 1, valor_de: 0, valor_ate: 2428.8, aliquota: 0, parcela_deduzir: 0, deducao_dependente: 189.59 },
  { ordem: 5, valor_de: 4664.68, valor_ate: null, aliquota: 0.275, parcela_deduzir: 908.73, deducao_dependente: 189.59 },
];
const redutor = { limite_isencao: 5000, limite_reducao: 7350 };
const faixas = { inss: inssFaixas, ir: irFaixas };

describe("calcularFerias", () => {
  it("30 dias sem abono: gozo + 1/3, INSS/IRRF sobre gozo+terco", () => {
    const r = calcularFerias({ base: 3000, diasGozo: 30, diasAbono: 0, diasDireito: 30,
      dependentes: 0, faixas, redutor });
    const lanc = Object.fromEntries(r.lancamentos.map((l) => [l.rubrica_codigo, l.valor]));
    expect(lanc["ferias_gozo"]).toBe(3000);
    expect(lanc["ferias_terco"]).toBe(1000);
    expect(r.base_inss).toBe(4000);
    expect(lanc["inss_13"]).toBeUndefined();
    expect(lanc["inss_ferias"]).toBeGreaterThan(0);
  });
  it("20 dias + 10 de abono: abono e seu terco sem incidencias", () => {
    const r = calcularFerias({ base: 3000, diasGozo: 20, diasAbono: 10, diasDireito: 30,
      dependentes: 0, faixas, redutor });
    const lanc = Object.fromEntries(r.lancamentos.map((l) => [l.rubrica_codigo, l.valor]));
    expect(lanc["ferias_gozo"]).toBe(2000);
    expect(lanc["ferias_terco"]).toBeCloseTo(666.67, 2);
    expect(lanc["abono_pecuniario"]).toBe(1000);
    expect(lanc["abono_terco"]).toBeCloseTo(333.33, 2);
    expect(r.base_inss).toBeCloseTo(2666.67, 2);
  });
  it("gozo+abono > direito = erro", () => {
    const r = calcularFerias({ base: 3000, diasGozo: 25, diasAbono: 10, diasDireito: 30,
      dependentes: 0, faixas, redutor });
    expect(r.validacoes.some((v) => v.nivel === "erro")).toBe(true);
  });
});

describe("descontoGozoNaMensal", () => {
  it("desconta os dias do mes ja pagos no recibo", () => {
    expect(descontoGozoNaMensal(3000, 20)).toBe(2000);
  });
  it("zero dias = zero", () => {
    expect(descontoGozoNaMensal(3000, 0)).toBe(0);
  });
});
```

Run: `npx vitest run src/lib/folha/engine/ferias.test.ts` — Expected: FAIL.

- [ ] **Step 2: Implementar**

```typescript
import { calcINSS, type InssBracket, type IrBracket } from "@/lib/payroll/calculators";
import { calcIrrf2026, type RedutorIrrf } from "./irrf";
import type { LancamentoCalculado, ValidacaoItem } from "./types";
import type { ResultadoEspecial } from "./decimo";

function round2(n: number): number {
  return Math.round(n * 100) / 100;
}

export function calcularFerias(args: {
  base: number; diasGozo: number; diasAbono: number; diasDireito: number;
  dependentes: number; faixas: { inss: InssBracket[]; ir: IrBracket[] }; redutor: RedutorIrrf | null;
}): ResultadoEspecial {
  const validacoes: ValidacaoItem[] = [];
  if (args.diasGozo + args.diasAbono > args.diasDireito)
    validacoes.push({ nivel: "erro", mensagem: `Gozo ${args.diasGozo} + abono ${args.diasAbono} > direito ${args.diasDireito}` });
  if (args.base <= 0) validacoes.push({ nivel: "erro", mensagem: "Base de férias zerada" });

  const gozo = round2((args.base / 30) * args.diasGozo);
  const terco = round2(gozo / 3);
  const abono = round2((args.base / 30) * args.diasAbono);
  const abonoTerco = round2(abono / 3);

  const baseInss = round2(gozo + terco);
  const inss = calcINSS(baseInss, args.faixas.inss);
  const baseIrrf = round2(Math.max(0, baseInss - inss));
  const irrf = calcIrrf2026({
    baseIr: baseIrrf, rendimento: baseInss, dependentes: args.dependentes,
    faixas: args.faixas.ir, redutor: args.redutor,
  });

  const lancamentos: LancamentoCalculado[] = [
    { rubrica_codigo: "ferias_gozo", referencia: `${args.diasGozo} dias`, valor: gozo, origem: "auto" },
    { rubrica_codigo: "ferias_terco", referencia: "1/3", valor: terco, origem: "auto" },
  ];
  if (args.diasAbono > 0) {
    lancamentos.push(
      { rubrica_codigo: "abono_pecuniario", referencia: `${args.diasAbono} dias`, valor: abono, origem: "auto" },
      { rubrica_codigo: "abono_terco", referencia: "1/3", valor: abonoTerco, origem: "auto" },
    );
  }
  lancamentos.push(
    { rubrica_codigo: "inss_ferias", referencia: null, valor: inss, origem: "auto" },
    { rubrica_codigo: "irrf_ferias", referencia: null, valor: irrf, origem: "auto" },
  );

  const total_proventos = round2(gozo + terco + abono + abonoTerco);
  const total_descontos = round2(inss + irrf);
  return {
    lancamentos, total_proventos, total_descontos,
    liquido: round2(total_proventos - total_descontos),
    base_inss: baseInss, base_irrf: baseIrrf, base_fgts: baseInss, validacoes,
  };
}

export function descontoGozoNaMensal(base: number, diasGozadosNoMes: number): number {
  if (diasGozadosNoMes <= 0) return 0;
  return round2((base / 30) * diasGozadosNoMes);
}
```

**Correção de spec embutida:** as rubricas de desconto de férias são `inss_ferias`/`irrf_ferias` (não reutilizam `inss_13`). Adicionar ao seed da Task 1:

```sql
  ('inss_ferias','INSS Férias','desconto','inss_ferias',false,false,false,60),
  ('irrf_ferias','IRRF Férias','desconto','irrf_ferias',false,false,false,61),
```

- [ ] **Step 3: Rodar** — Expected: PASS (5).
- [ ] **Step 4: Commit**

```bash
git add src/lib/folha/engine/ferias.ts src/lib/folha/engine/ferias.test.ts supabase/migrations/202606130001_folha_v21_ferias_13o.sql
git commit -m "feat(folha-v2.1): calculo de ferias com abono e desconto de gozo"
```

### Task 5: Serviço — períodos aquisitivos

**Files:**
- Create: `src/lib/folha/aquisitivos.ts`

- [ ] **Step 1: Implementar**

```typescript
import { createServerClient } from "@/lib/supabase/server";

export async function criarPeriodoInicial(contratoId: string) {
  const supabase = await createServerClient();
  const { data: contrato } = await supabase.from("folha_contratos")
    .select("data_admissao, janela_ferias").eq("id", contratoId).single();
  if (!contrato) throw new Error("Contrato não encontrado");
  const fim = new Date(`${contrato.data_admissao}T12:00:00Z`);
  fim.setUTCMonth(fim.getUTCMonth() + 12);
  await supabase.from("folha_periodos_aquisitivos").insert({
    contrato_id: contratoId, inicio: contrato.data_admissao,
    fim: fim.toISOString().slice(0, 10), janela: contrato.janela_ferias,
  });
}

export async function abrirProximoPeriodo(periodoId: string) {
  const supabase = await createServerClient();
  const { data: p } = await supabase.from("folha_periodos_aquisitivos")
    .select("contrato_id, fim, janela").eq("id", periodoId).single();
  if (!p) throw new Error("Período não encontrado");
  const fim = new Date(`${p.fim}T12:00:00Z`);
  fim.setUTCMonth(fim.getUTCMonth() + 12);
  await supabase.from("folha_periodos_aquisitivos")
    .insert({
      contrato_id: p.contrato_id, inicio: p.fim,
      fim: fim.toISOString().slice(0, 10), janela: p.janela,
    })
    .select("id").single();
}

export async function agendarGozo(periodoId: string, gozoInicio: string, gozoDias: number, diasAbono: number) {
  const supabase = await createServerClient();
  const { data: p } = await supabase.from("folha_periodos_aquisitivos")
    .select("dias_direito, status").eq("id", periodoId).single();
  if (!p) throw new Error("Período não encontrado");
  if (!["aberto", "agendado", "vencido"].includes(p.status)) throw new Error("Período já gozado");
  if (gozoDias + diasAbono > p.dias_direito)
    throw new Error(`Gozo ${gozoDias} + abono ${diasAbono} excede direito ${p.dias_direito}`);
  await supabase.from("folha_periodos_aquisitivos").update({
    gozo_inicio: gozoInicio, gozo_dias: gozoDias, dias_abono: diasAbono, status: "agendado",
  }).eq("id", periodoId);
}

export async function marcarVencidos(hojeISO: string) {
  const supabase = await createServerClient();
  const { data: abertos } = await supabase.from("folha_periodos_aquisitivos")
    .select("id, fim").eq("status", "aberto");
  const vencidos = (abertos ?? []).filter((p) => {
    const limite = new Date(`${p.fim}T12:00:00Z`);
    limite.setUTCMonth(limite.getUTCMonth() + 11);
    return new Date(`${hojeISO}T12:00:00Z`) > limite;
  });
  if (vencidos.length) {
    await supabase.from("folha_periodos_aquisitivos")
      .update({ status: "vencido" }).in("id", vencidos.map((v) => v.id));
  }
  return vencidos.length;
}
```

Integração: chamar `criarPeriodoInicial` na action de criação de contrato (Task 15 do plano v2 — adicionar uma linha após o insert do contrato). Jobs usam `createAdminClient` — adaptar: estas funções recebem o client como parâmetro opcional OU duplicar versão admin em `folha-jobs.ts`; decisão: aceitar `client` opcional `(client?: SupabaseClient)` com default `createServerClient()`, padrão único para todo o serviço v2.1.

- [ ] **Step 2: Typecheck + commit**

```bash
npm run typecheck
git add src/lib/folha/aquisitivos.ts
git commit -m "feat(folha-v2.1): servico de periodos aquisitivos"
```

### Task 6: Serviço — runs especiais + baixa de provisões + gozo na mensal

**Files:**
- Create: `src/lib/folha/runs-especiais.ts`
- Modify: `src/lib/folha/service.ts` (injeção `ferias_desconto_gozo`)
- Modify: `src/lib/actions/folha.ts` (fechamento baixa provisões de run especial; `validarRun` por tipo)

- [ ] **Step 1: Implementar `gerarRunEspecial`**

```typescript
import { createServerClient } from "@/lib/supabase/server";
import { getFaixasVigentes } from "@/lib/data/folha";
import { avos, baseCalculo13Ferias } from "./engine/avos";
import { calcularDecimo1a, calcularDecimo2a, type ResultadoEspecial } from "./engine/decimo";
import { calcularFerias } from "./engine/ferias";
import { recalcularTotaisRun } from "./service";

export async function gerarRunEspecial(
  companyId: string, competencia: string, tipo: "decimo_1a" | "decimo_2a" | "ferias",
  geradaPor: string, janelaCodigo?: string,
) {
  const supabase = await createServerClient();
  const existente = await supabase.from("folha_runs").select("id")
    .eq("company_id", companyId).eq("competencia", competencia).eq("tipo", tipo).maybeSingle();
  if (existente.data) return { runId: existente.data.id, criada: false };

  const [{ data: config }, faixas] = await Promise.all([
    supabase.from("folha_config").select("*").eq("company_id", companyId).single(),
    getFaixasVigentes(competencia),
  ]);
  if (!config) throw new Error("Config não encontrada");
  const anoRef = Number(competencia.slice(0, 4));

  let contratos: Array<{ contrato: ContratoEspecial; periodo?: PeriodoAgendado }> = [];
  if (tipo === "ferias") {
    contratos = await contratosComFeriasAgendadas(companyId, janelaCodigo, competencia);
  } else {
    contratos = await contratosCltAtivos(companyId, tipo, anoRef);
  }
  if (contratos.length === 0) return { runId: null, criada: false, motivo: "nenhum contrato elegível" };

  const { data: run, error } = await supabase.from("folha_runs")
    .insert({ escola_id: config.escola_id, company_id: companyId, competencia, tipo, gerada_por: geradaPor })
    .select("id").single();
  if (error) throw error;

  for (const { contrato, periodo } of contratos) {
    const basesAnteriores = await basesUltimos12(contrato.id, competencia);
    const base = baseCalculo13Ferias(contrato.perfil_codigo, config.base_13_ferias,
      contrato.salario_vigente, basesAnteriores);
    let resultado: ResultadoEspecial;
    if (tipo === "decimo_1a") {
      resultado = calcularDecimo1a({ base, avos: avos(contrato.data_admissao, contrato.data_desligamento, anoRef) });
    } else if (tipo === "decimo_2a") {
      const valor1a = await valor1aPagaNoAno(contrato.id, anoRef);
      resultado = calcularDecimo2a({
        base, avos: avos(contrato.data_admissao, contrato.data_desligamento, anoRef),
        valor1aPaga: valor1a, dependentes: contrato.dependentes_irrf,
        faixas: { inss: faixas.inss, ir: faixas.ir }, redutor: faixas.redutor,
      });
    } else {
      resultado = calcularFerias({
        base, diasGozo: periodo!.gozo_dias, diasAbono: periodo!.dias_abono,
        diasDireito: periodo!.dias_direito, dependentes: contrato.dependentes_irrf,
        faixas: { inss: faixas.inss, ir: faixas.ir }, redutor: faixas.redutor,
      });
      if (contrato.antecipa_13_com_ferias && !(await valor1aPagaNoAno(contrato.id, anoRef))) {
        const d1 = calcularDecimo1a({ base, avos: avos(contrato.data_admissao, contrato.data_desligamento, anoRef) });
        resultado.lancamentos.push(...d1.lancamentos);
        resultado.total_proventos = round2(resultado.total_proventos + d1.total_proventos);
        resultado.liquido = round2(resultado.liquido + d1.liquido);
        resultado.base_fgts = round2(resultado.base_fgts + d1.base_fgts);
      }
    }
    await persistirItemEspecial(run.id, contrato.id, resultado, periodo?.id);
  }
  await recalcularTotaisRun(run.id);
  return { runId: run.id, criada: true };
}
```

Helpers no mesmo arquivo (assinaturas que as queries devem cumprir — implementar com selects diretos):

```typescript
type ContratoEspecial = {
  id: string; perfil_codigo: string; salario_vigente: number;
  data_admissao: string; data_desligamento: string | null;
  dependentes_irrf: number; antecipa_13_com_ferias: boolean;
};
type PeriodoAgendado = { id: string; gozo_dias: number; dias_abono: number; dias_direito: number };

function round2(n: number): number { return Math.round(n * 100) / 100; }

async function contratosCltAtivos(companyId: string, tipo: string, anoRef: number) { /* contratos ativos com perfil clt/clt_professor; para decimo_1a, excluir quem ja tem lancamento decimo_1a_parcela em run fechada/aprovada do anoRef (query em folha_lancamentos join itens join runs) */ }
async function contratosComFeriasAgendadas(companyId: string, janela: string | undefined, competencia: string) { /* periodos status=agendado da janela cujo gozo_inicio cai no mes seguinte a competencia */ }
async function basesUltimos12(contratoId: string, competencia: string) { /* base_fgts dos itens de runs tipo=mensal status=fechada anteriores a competencia, ordem desc, limit 12, retorna number[] */ }
async function valor1aPagaNoAno(contratoId: string, anoRef: number) { /* soma de lancamentos decimo_1a_parcela do contrato em runs aprovada+ do ano */ }
async function persistirItemEspecial(runId: string, contratoId: string, r: ResultadoEspecial, periodoId?: string) { /* insert folha_itens com totais/bases + insert folha_lancamentos mapeando rubrica codigo->id (mesma mecanica de recalcularItemDb do service.ts v2 — copiar o bloco de mapeamento rubricaIds de la) */ }
```

ATENÇÃO: os corpos comentados acima DEVEM ser implementados nesta task (são 4 selects e 1 insert seguindo exatamente os padrões de `service.ts` v2); o plano define o contrato de cada um. Salário vigente: para `clt` = `salario_base`; para `clt_professor` = `valor_hora_aula × aulas_semanais × semanas_mes` + verbas contratuais de provento (reusar `calcHoraAula`).

- [ ] **Step 2: Fechamento de run especial** — em `transicionarRunAction` (v2), quando `destino === "fechada"` e `run.tipo !== "mensal"`: baixar provisões (`update folha_provisoes set baixada_em = now() where contrato_id in (itens da run) and tipo = 'decimo_terceiro'|'ferias' and baixada_em is null` — 13º baixa na `decimo_2a`, férias na `ferias`); para férias: marcar período `gozado` + `run_id` + chamar `abrirProximoPeriodo`. NÃO gravar provisões novas em run especial (só a mensal acumula).

- [ ] **Step 3: Gozo na mensal** — em `gerarRun`/`recalcularItemDb` (service.ts v2): antes de chamar `calcularItem`, buscar período `gozado` ou `agendado` cujo intervalo `[gozo_inicio, gozo_inicio + gozo_dias)` intersecta a competência; se existir, injetar manual `{ rubrica_codigo: "ferias_desconto_gozo", valor: descontoGozoNaMensal(base, diasNoMes), referencia: "X dias", origem: "auto" }` na lista de manuais (rubrica precisa existir em `rubricasExtras` — já vem de `todasRubricas`).

- [ ] **Step 4: `validarRun` por tipo** — adicionar: run `ferias` com item sem `periodoId` vinculado → erro; run `decimo_2a` com contrato que tem 1ª paga e item sem `desconto_adiantamento_13` → erro.

- [ ] **Step 5: Typecheck + commit**

```bash
npm run typecheck
git add src/lib/folha/runs-especiais.ts src/lib/folha/service.ts src/lib/actions/folha.ts
git commit -m "feat(folha-v2.1): runs especiais, baixa de provisoes e gozo na mensal"
```

### Task 7: Jobs do dispatcher

**Files:**
- Modify: `src/lib/actions/folha-jobs.ts`
- Modify: `src/app/api/jobs/dispatch/route.ts` (registrar 3 jobs novos)

- [ ] **Step 1: Implementar**

```typescript
export async function jobGerarDecimo(hoje: Date) {
  const supabase = createAdminClient();
  const hojeMMDD = hoje.toISOString().slice(5, 10);
  const competencia = hoje.toISOString().slice(0, 7);
  const { data: configs } = await supabase.from("folha_config").select("company_id, decimo_gerar_dia, jobs");
  const resultados = [];
  for (const cfg of configs ?? []) {
    for (const tipo of ["decimo_1a", "decimo_2a"] as const) {
      if (cfg.decimo_gerar_dia?.[tipo] === hojeMMDD) {
        resultados.push({ tipo, ...(await gerarRunEspecial(cfg.company_id, competencia, tipo, "cron")) });
      }
    }
  }
  return resultados;
}

export async function jobGerarFerias(hoje: Date) {
  const supabase = createAdminClient();
  const { data: configs } = await supabase.from("folha_config")
    .select("company_id, ferias_janelas, ferias_gerar_antes_dias");
  const resultados = [];
  for (const cfg of configs ?? []) {
    for (const janela of cfg.ferias_janelas ?? []) {
      const alvo = new Date(hoje);
      alvo.setUTCDate(alvo.getUTCDate() + cfg.ferias_gerar_antes_dias);
      if (alvo.getUTCMonth() + 1 === janela.mes_gozo && hoje.getUTCDate() === 1) {
        const competencia = hoje.toISOString().slice(0, 7);
        resultados.push({ janela: janela.codigo,
          ...(await gerarRunEspecial(cfg.company_id, competencia, "ferias", "cron", janela.codigo)) });
      }
    }
  }
  return resultados;
}

export async function jobAlertasAquisitivo(hoje: Date) {
  const supabase = createAdminClient();
  const hojeISO = hoje.toISOString().slice(0, 10);
  const vencidosNovos = await marcarVencidos(hojeISO);
  const { data: configs } = await supabase.from("folha_config").select("company_id, alerta_aquisitivo_dias");
  const marcos: number[] = configs?.[0]?.alerta_aquisitivo_dias ?? [60, 30];
  const { data: abertos } = await supabase.from("folha_periodos_aquisitivos")
    .select("id, fim, status, folha_contratos(employees(name))")
    .in("status", ["aberto", "vencido"]);
  const msgs: string[] = [];
  for (const p of abertos ?? []) {
    const limite = new Date(`${p.fim}T12:00:00Z`);
    limite.setUTCMonth(limite.getUTCMonth() + 11);
    const dias = Math.floor((limite.getTime() - new Date(`${hojeISO}T12:00:00Z`).getTime()) / 86400000);
    if (p.status === "vencido") msgs.push(`FÉRIAS VENCIDAS (pagto em dobro): ${p.folha_contratos.employees.name}`);
    else if (marcos.includes(dias)) msgs.push(`Férias de ${p.folha_contratos.employees.name} vencem em ${dias} dias sem agendamento`);
  }
  if (msgs.length) {
    await supabase.from("notificacoes").insert(msgs.map((mensagem) => ({
      titulo: "Períodos aquisitivos", mensagem, tipo: "folha", link: "/rh/folha-v2/ferias",
    })));
  }
  return { vencidosNovos, alertas: msgs.length };
}
```

No dispatcher, adicionar à lista `jobs`: `["folha_decimo", () => jobGerarDecimo(hoje)]`, `["folha_ferias", () => jobGerarFerias(hoje)]`, `["folha_aquisitivos", () => jobAlertasAquisitivo(hoje)]`. Nota: `marcarVencidos` e `gerarRunEspecial` precisam aceitar o admin client (ver decisão da Task 5).

- [ ] **Step 2: Teste manual** — `curl -H "Authorization: Bearer $CRON_SECRET" http://localhost:3000/api/jobs/dispatch` com data simulada (parametrizar `?hoje=2026-11-01` aceito só em dev) — Expected: run `decimo_1a` criada; segunda chamada `criada: false`.

- [ ] **Step 3: Commit**

```bash
git add src/lib/actions/folha-jobs.ts src/app/api/jobs/dispatch/route.ts
git commit -m "feat(folha-v2.1): jobs de 13o, ferias e alertas de aquisitivo"
```

### Task 8: Telas

**Files:**
- Create: `src/app/(app)/rh/folha-v2/ferias/page.tsx`
- Create: `src/components/folha/agendar-gozo-form.tsx`
- Create: `src/lib/actions/folha-especiais.ts`
- Modify: `src/app/(app)/rh/folha-v2/page.tsx` (badge `tipo` + botão "Gerar 13º/Férias")
- Modify: `src/app/(app)/rh/folha-v2/config/page.tsx` (grupo "Férias e 13º")
- Modify: telas de contrato (campos `antecipa_13_com_ferias`, `janela_ferias`)
- Modify: tela de provisões (coluna "baixado em" + link run)

- [ ] **Step 1: Actions** — `agendarGozoAction(formData)` (periodo_id, gozo_inicio, gozo_dias, dias_abono → `agendarGozo` + revalidate) e `gerarRunEspecialAction(formData)` (company_id, competencia, tipo → `gerarRunEspecial`), ambas com `requirePermission("rh.folha-v2", "update")`.

- [ ] **Step 2: Agenda de férias** — server component: fetch períodos com contrato/funcionário/janela; tabela com status colorido (`vencido` = vermelho via `StatusPill` tone danger); card topo "X períodos vencem em 60 dias"; cada linha `aberto|vencido` abre `AgendarGozoForm` (client: date + dias + abono select 0/10, submit → `agendarGozoAction`).

- [ ] **Step 3: Ajustes nas telas existentes** — badge do tipo na lista de runs (`mensal` sem badge; `13º 1ª`, `13º 2ª`, `Férias`); grupo de config; campos de contrato; coluna de provisões.

- [ ] **Step 4: Smoke + commit**

```bash
git add "src/app/(app)/rh/folha-v2" src/components/folha/agendar-gozo-form.tsx src/lib/actions/folha-especiais.ts
git commit -m "feat(folha-v2.1): agenda de ferias e ajustes de telas"
```

### Task 9: Recibos PDF + pacote do contador

**Files:**
- Modify: `src/lib/folha/holerite-pdf.ts`
- Modify: `src/lib/folha/pacote-contador.ts`

- [ ] **Step 1: Recibos** — `gerarHoleritePdf` ganha parâmetro `opcoes: { titulo: string; assinatura?: boolean; cabecalhoExtra?: string[] }`. Run `ferias` → título "Recibo de Férias", `cabecalhoExtra` = [`Período aquisitivo: DD/MM/AAAA a DD/MM/AAAA`, `Gozo: DD/MM/AAAA a DD/MM/AAAA (N dias)`], `assinatura: true` (linha + "Assinatura do funcionário" a ~30mm do rodapé). Runs `decimo_*` → título "Recibo 13º Salário — 1ª/2ª parcela". Mensal inalterado (layout oficial do Adendo 1 da v2).

- [ ] **Step 2: Pacote** — aba `Ferias-13o` quando a competência tem run especial: colunas Funcionário | CPF | Tipo | Base | Memória da média (string "12×: 4.900,00; 4.850,00; ..." quando `media_12`) | Avos | Proventos | INSS | IRRF | Líquido.

- [ ] **Step 3: Smoke + commit**

```bash
git add src/lib/folha/holerite-pdf.ts src/lib/folha/pacote-contador.ts
git commit -m "feat(folha-v2.1): recibos de ferias/13o e aba no pacote do contador"
```

### Task 10: Import de bases históricas (condicional) + verificação final

**Files:**
- Create: `scripts/importar_bases_historicas.mjs`

- [ ] **Step 1: Script one-off** (só se a escola decidir importar 2025/26 para a média — pendência 5 da spec): lê xlsx (colunas: cpf, competencia, base_fgts), valida, e insere runs sintéticas `tipo='mensal'` `status='fechada'` `gerada_por='import-historico'` com um item por contrato contendo apenas `base_fgts` (sem lançamentos). Flags `--dry-run` (default) e `--apply`, seguindo o padrão de `scripts/reconcile_matriculas_2026.mjs`. `basesUltimos12` já as encontra naturalmente.
- [ ] **Step 2:** `npm run typecheck && npm run lint && npm run test` — Expected: verde.
- [ ] **Step 3:** Smoke E2E com datas simuladas: jobs geram `decimo_1a` (01/11), `decimo_2a` (01/12), férias (01/06 p/ julho); fechar férias baixa provisão e abre período novo; folha mensal de julho recebe `ferias_desconto_gozo`.
- [ ] **Step 4: Commit final**

```bash
git add scripts/importar_bases_historicas.mjs
git commit -m "feat(folha-v2.1): import de bases historicas e verificacao final"
```

---

## Cobertura da spec (self-review)

| Requisito | Task |
|---|---|
| `tipo` em runs + unique triplo | 1 |
| `folha_periodos_aquisitivos` + backfill | 1, 5 |
| Campos contrato/config + rubricas seed (incl. `inss_ferias`/`irrf_ferias`) | 1, 4 |
| avos / média 12 / base por perfil | 2 |
| 13º exclusivo (1ª sem descontos, 2ª com INSS/IRRF+redutor, desconto 1ª) | 3 |
| Férias + abono sem incidências + validações | 4 |
| Antecipação 1ª parcela com férias | 6 |
| Baixa de provisões + abrir período seguinte + gozo na mensal | 6 |
| Jobs gerar/alertas (incl. vencido = dobro) | 7 |
| Telas (agenda, badges, config, contrato, provisões) | 8 |
| Recibos PDF (assinatura) + aba contador (memória da média) | 9 |
| Histórico para média (pendência 5) | 10 |
| Validações por tipo bloqueando fechamento | 6 (step 4) |

Pendências da spec que NÃO viram task (decisões externas): confirmar documento da CCT de educação básica (piso, hora-atividade); regra vigente×média quando aulas variam (pendência 6 da spec).

---

## ADENDO 1 (2026-06-12) — recibo oficial de férias, fórmula IRRF e CCT

Aplicar junto com as tasks indicadas (detalhes na spec, seção "Adendo 1"; fórmula IRRF no ADENDO 2 do plano v2):

- **Tasks 3 e 4 (fixtures de teste):** objeto `redutor` ganha os coeficientes oficiais: `{ limite_isencao: 5000, limite_reducao: 7350, coef_fixo: 978.62, coef_mult: 0.133145 }`. O teste "redutor Lei 15.270 aplica no 13o" continua válido (asserções por desigualdade).
- **Task 1 (migration), acrescentar:**

```sql
alter table public.folha_config
  add column if not exists recesso jsonb not null default '{"inicio":"12-21","fim":"01-10"}';
alter table public.companies
  add column if not exists endereco text,
  add column if not exists cidade text;
```

E ao seed de rubricas:

```sql
  ('ferias_dobro','Dobro de férias (art. 137)','provento','manual',true,true,true,20),
  ('ferias_dobro_terco','1/3 do dobro','provento','manual',true,true,true,21),
```

- **Task 5 (`agendarGozo`):** validação adicional — `gozo_inicio` não pode ser sábado, domingo ou feriado: reusar `isDiaUtil` de `src/lib/folha/date-utils.ts` com os `feriados_locais` da config; lançar erro "Início do gozo deve ser dia útil (CCT/art. 134)".
- **Task 8 (agenda):** exibir faixa de recesso da config como banner informativo ("Recesso: 21/12 a 10/01 — sem convocação, não conta como férias").
- **Task 9 (recibo de férias):** layout oficial em duas partes — "Aviso Prévio de Férias" (períodos, bloco Base para cálculo com faltas/salário/médias/vantagens, bloco Proventos e Descontos, texto legal, CIENTE com 2 assinaturas) + "Recibo de Férias" (quitação com razão social, endereço e cidade de `companies`, valor por extenso, data, assinatura). Valor por extenso: implementar `valorPorExtenso(n: number): string` em `src/lib/folha/extenso.ts` com teste (`5645.22 → "cinco mil seiscentos e quarenta e cinco reais e vinte e dois centavos"`).
- **Task 10 (verificação):** caso dourado adicional de férias com tabela 2025 SEM redutor (recibo oficial jul/2025): base 5.531,40 → férias 5.531,40 + terço 1.843,80; INSS 842,11 (faixas 2025); IRRF 887,87; líquido 5.645,22. Exige seed de faixas 2025 com vigência `valido_de 2025-05-01 / valido_ate 2025-12-31` (IRRF 27,5% parcela 908,73) para o teste de vigência funcionar.
- **Config das empresas DESTA escola:** `percentual_hora_atividade = 0` (contracheque oficial não tem a rubrica); default genérico 5% permanece para empresas novas.

